# rmail thin client — Windows

See [docs/thin-client.md](../../docs/thin-client.md) for full setup and usage instructions.

Quick start:

```cmd
install.bat
run.bat --host YOUR_HOME_IP --port 8025 --token YOUR_TOKEN
```
